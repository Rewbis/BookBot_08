.env
.venv/
__pycache__/
*.pyc
node_modules/

# ── Book content stays local. Nothing the author writes, feeds in, or the agents
#    produce is committed: snapshots, per-call prompt/output logs, private notes,
#    style samples, illustrations. Code, docs, and tests only.
projects/
logs/
author_notes/
styles/
illustrations/
*.snapshot.json
.idx/
